import { IntroSection, IntroCard } from "../../components/IntroCard";
import { Callout, Tabs, Tab } from "nextra-theme-docs";
import { CodeBlock } from "../../components/CodeBlock";

# 安装

在项目中安装`Sandpack`依赖

<Tabs items={["npm", "yarn", "pnpm"]}>

  <Tab>
```bash
npm i @codesandbox/sandpack-react
```
  </Tab>

  <Tab>
```bash
yarn add @codesandbox/sandpack-react
```
  </Tab>

  <Tab>
```bash
pnpm add @codesandbox/sandpack-react
```
  </Tab>

</Tabs>

所有组件和打包器都打包在 `Sandpack` 组件中，该组件是该包的命名导出。除此之外，该包还包含多个**组件**, **实用工具** 和 **类型**。

## 快速入门

这将为您提供一个美观的代码编辑器，并在浏览器中运行和预览。

<CodeBlock stack>
{`import { Sandpack } from "@codesandbox/sandpack-react";

export default function App() {
  return <Sandpack />
}
`}
</CodeBlock>

## CDN

要将 Sandpack 与 CDN 一起使用，只需在 HTML 文件中包括 Sandpack 标签并指定包括 Sandpack 及其依赖项的 CDN 导入。

<br />

```html
<!DOCTYPE html>
<html>
  <head>
    <meta charset="UTF-8" />

    <script type="importmap">
      {
        "imports": {
          "react": "https://esm.sh/react@18.2.0",
          "react-dom": "https://esm.sh/react-dom@18.2.0",
          "react-dom/": "https://esm.sh/react-dom@18.2.0/",
          "@codesandbox/sandpack-react": "https://esm.sh/@codesandbox/sandpack-react@2.8.0"
        }
      }
    </script>

    <script type="module">
      import React from "react";
      import { createRoot } from "react-dom/client";
      import { Sandpack } from "@codesandbox/sandpack-react";

      const root = createRoot(document.getElementById("root"));
      const sandpackComponent = React.createElement(
        Sandpack,
        { template: "react" },
        null
      );
      root.render(sandpackComponent);
    </script>
  </head>

  <body>
    <div id="root"></div>
  </body>
</html>
```

## 下一步

<IntroSection>
  <IntroCard
    icon="Code"
    title="模板和文件 (Templates & files)"
    description="了解如何将文件添加到您的项目中并快速开始编码"
    href="/docs/getting-started/usage"
  />

  <IntroCard
    icon="Theme"
    title="主题 (Themes)"
    description="设计和定制您自己的主题并查找其他 Sandpack 预设"
    href="/docs/getting-started/themes"
  />
</IntroSection>
